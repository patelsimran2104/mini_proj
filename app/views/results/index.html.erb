<ul>
  <% @results.each do |result| %>
    <li>Subject:
      <%= result.subjects %> 
      Gread:
      <%= result.gread %>   <%= link_to 'Edit', edit_student_result_path(id: result.id, student_id: result.student_id) %>
    </li>
    </a>  
      
  <% end %>
</ul>

<%= link_to "add student's result", new_student_result_path(@student)%><br>
<%= link_to 'Back to homepage', student_path(params[:student_id]) %>


