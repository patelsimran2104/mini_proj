
  <tr>
    <td><%= result.subjects %></td>
    <td colspan="15"></td>
    <td><%= result.gread %></td>
    <td colspan="15"></td>
    <td><%= link_to 'Edit', edit_student_result_path(id: result.id, student_id: result.student_id) %></td>
    <td><%= link_to 'destroy', delete_result_path(id: result.id, student_id: result.student_id), method: :delete, data: {confirm: 'Are you sure?', remote: true}, class: "delete" %></td>
  </tr>
